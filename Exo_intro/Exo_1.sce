// Effectuer les opérations suivantes dans l'éditeur et afficher les résultats dans la console :
// -------------------------------------------------------------------
// > Calcul dans l'éditeur
a = 22/7;
b = 4^2 + 5;
c = (5 + 4^2)/(7 + 4^3);

// > Affichages dans la console 

disp(a)
disp(b)
disp(c)

// Pour n'effectuer les calculs que dans la console, bah c'est simple. Juste besoin de copier les lignes a, b et c (pas besoin de disp())
